name: psalm_91
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ Firebase (pinned to stable, gRPC-safe versions)
  firebase_core: ^2.10.0
  firebase_auth: ^4.7.0
  cloud_firestore: ^4.8.0
  firebase_messaging: ^14.6.0
  firebase_analytics: ^10.5.0
  firebase_storage: ^11.2.0

  # ✅ Supporting packages
  flutter_local_notifications: ^17.1.0
  flutter_native_splash: ^2.3.10
  provider: ^6.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/
